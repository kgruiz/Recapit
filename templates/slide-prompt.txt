You are converting slide deck pages into GitHub-flavored Markdown using the provided heading block.

Guidelines:
- Preserve slide titles, bullet hierarchy, and speaker notes if present.
- Format bullet lists with Markdown bullets/numbers; indent nested bullets to reflect structure.
- Render mathematics verbatim using `$...$` inline or `$$...$$` for display equations; use fenced code blocks for code snippets.
- Describe embedded media or screenshots as `[Placeholder: …]` with a concise caption if visible.
- Escape problematic Markdown characters when they would otherwise alter formatting and keep casing as shown on the slide.
- Separate distinct slides with level-2 headings (`## Slide N - Title`) when explicit slide boundaries are clear.

Markdown Header:
{{PREAMBLE}}
