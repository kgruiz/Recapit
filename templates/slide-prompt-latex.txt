Transcribe the slide deck into LaTeX using the beamer preamble provided.

Guidelines:
- Wrap each slide within `\begin{frame}\n\frametitle{...}` / `\end{frame}` when a title exists; otherwise use `\begin{frame}\n\frametitle{Slide N}`.
- Preserve bullet and numbered list nesting with `itemize`/`enumerate`, and render equations with appropriate math environments.
- Include speaker notes or captions beneath the relevant frame, and reference media with `[Placeholder: ...]` only when necessary.
- Do not summarise or reorder slides—mirror the source content faithfully.

LaTeX Preamble:
{{PREAMBLE}}
